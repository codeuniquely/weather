import '@testing-library/jest-dom';
import 'jest-preset-angular/setup-jest';
import 'whatwg-fetch';

// keep-comment: MSW set-up for JEST - must run before it can be used
// import './jest.msw.setup';
